import { auth } from "../srpcd/rpc.server.bundle";
import AuthUserInterceptor = auth.AuthUserInterceptor;
export declare class AuthUserInterceptorImpl extends AuthUserInterceptor {
    intercept(): Promise<any>;
}
