import { ClientPlatforms, ServerPlatforms } from "./lib";
declare type BuildProps = {
    entry: string;
    serverOutDir: string;
    clientOutDir: string;
    clientPlatform: ClientPlatforms;
    serverPlatform: ServerPlatforms;
};
export declare function generateCode({ serverOutDir, clientOutDir, clientPlatform, serverPlatform, entry }?: BuildProps): Promise<void>;
export {};
