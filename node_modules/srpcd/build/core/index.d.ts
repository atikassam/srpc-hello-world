export declare enum ClientPlatforms {
    WEB = "web",
    NODEJS = "nodejs"
}
export declare enum ServerPlatforms {
    NODEJS = "nodejs"
}
export interface WriterOptions {
    files?: WriterFile[];
    dir?: string;
}
export interface WriterFile {
    name: string;
    content: string;
    language: string;
}
export declare class ProjectDirectory {
    private options;
    private funcs;
    private dir;
    constructor(options: WriterOptions);
    removeFiles(filenames: string | string[]): this;
    writeFiles(files: WriterFile | WriterFile[]): Promise<any>;
    write(): Promise<any>;
    getDir(): string;
    private runPipe;
}
